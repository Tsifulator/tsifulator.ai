# Required
OPENAI_API_KEY=

# Server
NODE_ENV=development
PORT=4000
HOST=0.0.0.0
LOG_LEVEL=debug

# Auth — MUST change in production
JWT_DEV_SECRET=change_me_in_beta

# Database
DB_PATH=./data/tsifulator.db

# CORS — set to your frontend origin in production (empty = disabled)
CORS_ORIGIN=*
