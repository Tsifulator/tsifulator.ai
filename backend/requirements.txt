# Tsifulator.ai — Python Dependencies
# Install with: pip install -r requirements.txt

# Web server (the brain's API layer)
fastapi==0.115.0
uvicorn==0.30.6

# Anthropic (Claude — the AI engine)
anthropic==0.49.0

# Database + Vector Memory (Supabase)
supabase==2.7.2

# Environment variable management
python-dotenv==1.0.1

# Excel file manipulation (for local .xlsx files)
openpyxl==3.1.5

# Charts & Visualization (optional — install manually if needed)
# matplotlib>=3.8.0
# numpy>=1.24.0

# Utilities
httpx==0.27.2
# HTML→text parser for the SEC URL fetcher (10-K/10-Q HTML can hit 5MB raw;
# we strip to clean text before sending to Claude so we don't blow the
# context window).
beautifulsoup4==4.12.3
pydantic==2.8.2
psutil==5.9.8
yfinance==0.2.54
